# ブログ記事案: WSL環境におけるPowerShell実行の罠 - SecurityErrorとParserErrorとの長い戦いの記録

## 2025年7月10日

### はじめに

本プロジェクトでは、CSVで管理されたWBS（Work Breakdown Structure）を、独自のMarkdown形式に変換するPowerShellスクリプトを開発している。開発環境はWSL (Windows Subsystem for Linux) を使用しており、ここからWindowsネイティブのPowerShell (`pwsh.exe`) を呼び出す必要があった。

単純に見えたこのタスクは、WSLとWindowsの境界をまたぐことで発生する、複雑で発見しにくい問題の連続だった。本稿は、そのデバッグの道のりを記録したものである。

### 第一の壁: 回避できない `SecurityError`

最初に直面したのは、PowerShellの実行ポリシーに起因する `SecurityError` だった。

**現象:**
WSLから `cmd.exe /c "pwsh -File C:\path\to\script.ps1"` という、ごく標準的な方法でスクリプトを実行しようとすると、「スクリプトはデジタル署名されていません」という趣旨の `SecurityError` で実行がブロックされた。

**試したこと:**
1.  `Set-ExecutionPolicy RemoteSigned` や `-ExecutionPolicy Bypass` といった、一般的な実行ポリシーの回避策をすべて試したが、効果はなかった。WSL経由の呼び出しでは、これらの設定が正しく認識されていないようだった。
2.  次に、スクリプトファイルを直接実行する `-File` の代わりに、スクリプトの内容をすべて文字列として読み込み、`-Command` で実行する方法を試した。
    ```bash
    script_content=$(cat script.ps1)
    cmd.exe /c "pwsh -ExecutionPolicy Bypass -Command \"& { $script_content }\""
    ```
    この方法で、しつこかった `SecurityError` をついに回避できた。しかし、安堵したのも束の間、新たな問題が発生した。

### 第二の壁: 沈黙のエラー

`SecurityError` は消えたが、スクリプトは終了コード `1` で失敗するようになった。しかし、**エラーメッセージが一切表示されない**のだ。`Stderr` は空で、何が原因で失敗しているのか全く分からない、「沈黙のエラー」に直面した。

**試したこと（すべて失敗）:**
1.  **`try...catch` でのエラー捕捉:** スクリプト内の `catch` ブロックで `Write-Error` や `ThrowTerminatingError` を使ってエラーの詳細を出力しようとしたが、何も表示されなかった。
2.  **`cmd.exe` でのリダイレクト:** `... 2> stderr.log` のようにリダイレクトを試みたが、空のファイルが作られるか、あるいはファイル自体が生成されなかった。
3.  **PowerShell内部でのリダイレクト:** `catch` ブロック内で `$_ | Out-File C:\path\to\error.log` のように、エラーオブジェクトを直接ファイルに書き込もうとしたが、これも失敗に終わった。

**考察:**
この時点で、問題はスクリプトのロジックではなく、**`cmd.exe` を介して `-Command` で巨大なスクリプト文字列を渡すという実行方法そのものにある**と推測した。引用符のエスケープ処理や、WSLとcmdの間の文字列の受け渡しプロセスが、PowerShellエンジンのエラー出力メカニズムを破壊しているのではないかと考えた。

### 第三の壁とブレークスルー: `ParserError` の発見

デバッグの原点に立ち返り、アプローチを根本から見直した。

**アプローチの転換:**
1.  `-Command` の使用を諦め、`-File` での実行に戻す。
2.  `cmd.exe` の介在が怪しいと判断し、WSLから **`pwsh.exe` を直接呼び出す**ことにした。

```bash
# cmd.exe を使わず、直接 pwsh.exe を呼び出す
pwsh.exe -ExecutionPolicy Bypass -File "C:\path\to\script.ps1" ...
```

このコマンドを実行したところ、ついに、ついに `stderr.log` に**明確なエラーメッセージ**が記録された！

**エラー内容:**
```
ParserError: Unexpected attribute 'CmdletBinding'.
```

これは、`[CmdletBinding()]` という属性が予期しない場所にある、という単純な**構文エラー**だった。

### 根本原因と解決

エラーメッセージを元にスクリプトを確認すると、原因はすぐに判明した。デバッグの過程で、UIの言語を英語に設定するためのコード (`[System.Threading.Thread]::...`) をスクリプトの**1行目**に追加していたのだ。

PowerShellのルールでは、`[CmdletBinding()]` と `param()` ブロックは、スクリプトの**先頭**に記述しなければならない。このルールを破ったため、PowerShellパーサーがスクリプトを読み込んだ瞬間に構文エラーで停止していたのだ。

**なぜ、この単純なエラーの発見にこれほど時間がかかったのか？**
それは、`cmd.exe` を介した `-Command` という実行方法が、この基本的な `ParserError` のメッセージすら握りつぶし、我々を届けてくれなかったからだ。最初の `SecurityError` という派手な問題に気を取られ、その裏に隠れていた、より根源的で単純なミスを見抜けずにいた。

**最終的な解決策:**
1.  構文エラーを修正。(`[CmdletBinding()]` と `param()` がスクリプトの先頭に来るようにコードの順序を入れ替えた)
2.  WSLからは `cmd.exe` を介さず、`pwsh.exe` を直接呼び出す方式を採用。

この2つの対策により、スクリプトはついに正常に動作した。

### まとめと教訓

この長い戦いから得られた教訓は大きい。
1.  **WSL/Windows間の連携では `cmd.exe` の介在を疑え:** `cmd.exe` は、引用符やパスの解釈で予期せぬ問題を起こすことがある。可能な限り、実行したいプログラム (`pwsh.exe` など) を直接呼び出すべきだ。
2.  **エラーが出ないときは、エラー出力経路を疑え:** 「沈黙のエラー」は、エラーがないのではなく、エラーメッセージが途中で消失している可能性が高い。リダイレクトの方法や、呼び出し方法自体を変えてみる価値がある。
3.  **行き詰まったら原点に戻れ:** 複雑な問題に行き詰まったときは、最小限のコード (`"Hello, World"` を表示するだけ、など) で実行を試みるのが有効。どこで問題が発生しているのかを切り分けることができる。
4.  **エラーは英語で:** `[System.Threading.Thread]::CurrentThread.CurrentUICulture = 'en-US'` をスクリプトの `begin` ブロックに入れておけば、エラーメッセージが英語になり、文字化けを防ぎ、ウェブで検索しやすくなる。

WSLは非常に強力なツールだが、その境界をまたぐときには、特有の「お作法」が存在することを痛感させられた一件だった。
